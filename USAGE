USAGE

    To display results:

        make show

    To re-run all tests:

        make run

    To run test and display commentary:

        cd bin/
        ./run.sh <test>.sh ...

    For low level control:

        cd bin/
        ./<test>.sh

        # to chnage loop count
        loop_max=200 ./<test>.sh

    WARNING: The individual test files
    are not designed to work correctly with
    calls like:

        bash bin/<file>.sh

End of file
